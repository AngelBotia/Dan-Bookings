
export class filesModel{
    saveImg = async ({ limit, page }) => {
        try {

            // return rows

        } catch (error) {
            console.error(error.message);
            throw new Error("works dont found")
        }
    };
}